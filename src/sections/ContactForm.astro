---
import { Icon } from "astro-icon/components"
import { Image } from "astro:assets"
import LogoDark from "../../src/assets/LogoDark.svg";
import Input from "../components/Input.astro";
import Button from "../components/Button.astro";

---

<div class="wrapper" id="formSection">
    <div class="infoWrapper">
        <span class="infoTitle">Ukończ cele swojej firmy z Vitresoft</span>
        <span class="infoDesc">Otrzymasz odpowiedź na podany adres email w przeciągu 24 godzin. Odpowiemy na wszystkie nurtujące pytania i zaproponujemy rozwiązania do przedstawionego budżetu</span>
    </div>
    <div class="formWrapper">
        <div class="formTopBar">
            <Image src={LogoDark} alt="logo" width={30} height={30} />
        </div>
        <form class="formContainer">
            <div class="formSections">
                <div class="formTopSection">
                    <div class="formTopSectionInput">
                        <Input type="text" placeholder="Name" name="name" />
                    </div>
                    <div class="formTopSectionInput">
                        <Input type="text" placeholder="Surname" name="surname" />
                    </div>
                    <div class="formTopSectionInput">
                        <Input type="tel" placeholder="+48 123 456 789" name="tel" />
                    </div>
                    <div class="formTopSectionInput">
                        <Input type="text" placeholder="Subject" name="subject" />
                    </div>
                </div>
                <div class="formBottomSection">
                    <Input type="email" placeholder="E-mail" name="email" />
                    <Input type="text" placeholder="Company" name="company" />
                    <Input type="text" placeholder="Content" name="content" />
                </div>
            </div>
            <div class="formButtonContainer">
                <Button variant="primary" id="formSubmitButton">Send</Button>
            </div>
        </form>
    </div>
    <div class="contactWrapper">
        <span class="contactRow">
            <Icon name="uil:phone" />
            +123 456 789
        </span>
        <span class="contactRow borderRow">
            <Icon name="uil:envelope-alt" />
            biuro@vitresoft.com
        </span>
        <span class="contactRow">
            <Icon name="uil:map-marker" />
            ul. Pana Balcera 6/88 20-631 Lublin
        </span>
    </div>
</div>

<style>
    .wrapper{
        width: 900px;
        height: 400px;

        display: flex;
        flex-direction: column;
        justify-content: space-between;
        
        position: relative;

        border-radius: 30px;

        background-image: linear-gradient(45deg, rgba(123, 17, 249, 1), rgba(121, 34, 216, 1) 60%, rgb(64, 64, 217));
        color: var(--light);
        padding: 30px;

        margin-top: 70px;
    }

    .infoWrapper{
        display: flex;
        flex-direction: column;
        gap: 5px;

        width: 50%;
        height: 50%;
    }

    .infoTitle{
        font-weight: 500;
        font-size: 36px;
    }

    .infoDesc{
        font-weight: 300;
        font-size: 18px;
        color: rgba(255,255,255,0.7);
    }

    .formWrapper{
        width: 45%;
        height: 90%;

        display: flex;
        flex-direction: column;

        border-radius: 20px;

        position: absolute;
        top: 50%;
        right: 2.5%;

        transform: translateY(-50%);
        
        background-color: var(--light);
    }

    .formTopBar{
        height: 10%;
        padding: 20px 20px 0px;
    }

    .formContainer{
        width: 100%;
        height: 90%;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
    }

    .formSections{
        display: flex;
        flex-direction: column;
    }

    .formTopSection{
        padding: 20px 20px 0px;

        display: flex;
        flex-direction: row;
        flex-wrap: wrap;
        justify-content: space-between;
    }

    .formTopSectionInput{
        width: 48%;
    }

    .formBottomSection{
        padding: 0px 20px 20px;

        display: flex;
        flex-direction: column;
    }

    .formButtonContainer{
        padding: 20px;

        display: flex;
        justify-content: center;
        align-items: center;
    }

    .contactWrapper{
        width: 50%;
        height: 50%;

        display: flex;
        flex-direction: column;
        gap: 5px;
        justify-content: center;
    }

    .contactRow{
        width: max-content;
        padding: 10px;

        display: flex;
        flex-direction: row;
        gap: 5px;
    }

    .borderRow{
        border: 1px solid var(--light);
        background-color: rgba(255,255,255,0.1);
        border-radius: 8px;
    }

    @media only screen and (max-width: 1115px){
        .wrapper{
            width: 80%;
            height:unset;
            padding: 30px;
            gap: 30px;
        }

        .formWrapper{
            position: unset;
            transform: unset;
        }

        .infoWrapper, .contactWrapper, .formWrapper{
            width: 100%;
        }
    }
</style>